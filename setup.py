from setuptools import setup, find_packages

setup(
    name="pigeon_roost_counter",
    version="0.1.0",
    author="Mustafa",
    packages=find_packages(),
    install_requires=[
        # Örneğin: "torch", "numpy"
    ],
    python_requires=">=3.6",
)
